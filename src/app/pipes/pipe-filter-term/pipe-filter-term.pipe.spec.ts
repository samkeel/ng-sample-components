import { PipeFilterTermPipe } from './pipe-filter-term.pipe';

describe('PipeFilterTermPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterTermPipe();
    expect(pipe).toBeTruthy();
  });
});
